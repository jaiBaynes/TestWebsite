<div class="container">
  <div class="header">
    <h1>Image Galleries</h1>
  </div>

  <% if @galleries.any? %>
    <div class="galleries-grid">
      <% @galleries.each do |gallery| %>
        <div class="gallery-card">
          <h2><%= link_to gallery.title, gallery_path(gallery) %></h2>
          <p><%= truncate(gallery.description, length: 100) %></p>
          <p class="gallery-stats"><%= gallery.images.count %> characters</p>
          <div class="gallery-actions">
            <%= link_to 'View Gallery', gallery_path(gallery), class: 'btn btn-primary' %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="no-galleries">No galleries available.</p>
  <% end %>
</div>
